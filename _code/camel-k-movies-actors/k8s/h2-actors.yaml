apiVersion: apps/v1
kind: Deployment
metadata:
  name: h2-actors
  labels:
    app: h2-actors
spec:
  replicas: 1
  selector:
    matchLabels:
      app: h2-actors
  template:
    metadata:
      labels:
        app: h2-actors
    spec:
      containers:
      - name: h2
        image: kamelets-movies-actors-demo-h2-actors:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 9092
          name: tcp
        - containerPort: 8082
          name: web
        env:
        - name: INIT_SQL
          value: "/opt/init/actors.sql"
        - name: DB_NAME
          value: "actors"
        volumeMounts:
        - name: init-sql
          mountPath: /opt/init
      volumes:
      - name: init-sql
        configMap:
          name: actors-init-sql
---
apiVersion: v1
kind: Service
metadata:
  name: h2-actors
spec:
  selector:
    app: h2-actors
  ports:
  - port: 9092
    targetPort: 9092
    name: tcp
  - port: 8082
    targetPort: 8082
    name: web